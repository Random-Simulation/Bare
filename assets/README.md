# assets/

Bundled binary assets shipped with Bare.

## models/
- `SupraTitle-50M-Q8_0.gguf` (~56 MB) — local CPU title-generation model,
  loaded by `title-gen.cjs` via `node-llama-cpp`.

These files are **intentionally committed** (the app needs them at runtime).
Binary — do not edit by hand.
